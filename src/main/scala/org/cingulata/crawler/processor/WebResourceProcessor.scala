package org.cingulata.crawler.processor

/**
  * Created by kuzmentsov on 30.10.2016.
  */
class WebResourceProcessor {
  def parseTitle(initial: String): String = initial
  def parseFeatureName(initial: String): String = initial
  def parseManufacturer(initial: String): String = initial
}
